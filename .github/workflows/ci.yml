name: CI

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  test-and-build:
    name: CI Test & Build
    uses: ./.github/workflows/build-ios.yml
    with:
      upload_artifact: false
      version: '1.0.0'
